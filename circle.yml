dependencies:
    pre:
        - sudo apt-get install texlive
test:
    pre:
        - make
        - perl scripts/latexcount.pl paper.tex
        - perl scripts/texcount.pl paper.tex

    post:
        - cp paper.pdf $CIRCLE_ARTIFACTS/paper.pdf
